# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Test / coverage artifacts
TestResults/
*.xcresult
*.profraw
*.profdata
coverage/

# macOS
.DS_Store

# SwiftPM / local build caches
.build/
.swiftpm/
Packages/
.derivedData/

# Temporary speech synthesis intermediates (fixture source only; do not commit)
*.aiff
*.AIFF

# Keep Timbre.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved tracked
# so FluidAudio stays pinned for the Parakeet smoke-test target.

# Local OAuth / API configuration (may contain environment-specific client IDs)
Config/Auth.local.xcconfig
